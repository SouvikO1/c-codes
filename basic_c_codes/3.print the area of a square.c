//print the area of a square(3)
#include <stdio.h>

int main()
{
    float side;
    printf("enter the values of the side:");
    scanf("%f",&side);
    printf("the area of the square:%f",(side*side));

    return 0;
}

